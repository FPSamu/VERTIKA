<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Recuperar Contraseña - VERTIKA</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to bottom, #4e7ed0, #2a3e8c);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      color: #333;
    }

    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&w=1470&q=80') center/cover no-repeat;
      opacity: 0.2;
      z-index: -1;
    }

    .container {
      background: rgba(255, 255, 255, 0.95);
      padding: 40px 30px;
      border-radius: 12px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
      width: 360px;
      text-align: center;
    }

    .icon {
      font-size: 50px;
      margin-bottom: 20px;
    }

    h1 {
      margin-bottom: 15px;
      color: #2c3e50;
      font-size: 26px;
    }

    .description {
      font-size: 14px;
      color: #666;
      margin-bottom: 25px;
      line-height: 1.5;
    }

    form input {
      width: 100%;
      padding: 14px 4px;
      margin: 8px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 15px;
      transition: border 0.3s, box-shadow 0.3s;
    }

    form input:focus {
      border-color: #667eea;
      box-shadow: 0 0 5px rgba(102, 126, 234, 0.5);
      outline: none;
    }

    form button {
      width: 100%;
      padding: 14px;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 15px;
      transition: background 0.3s;
    }

    form button:hover {
      background: linear-gradient(135deg, #5a67d8, #613abf);
    }

    form button:disabled {
      background: #ccc;
      cursor: not-allowed;
    }

    .message {
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 15px;
      font-size: 14px;
    }

    .message.error {
      background: #ffe6e6;
      color: #e74c3c;
      border: 1px solid #e74c3c;
    }

    .message.success {
      background: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
    }

    .back-link {
      margin-top: 20px;
      font-size: 14px;
      color: #555;
    }

    .back-link a {
      color: #667eea;
      font-weight: bold;
      text-decoration: none;
      transition: color 0.3s;
    }

    .back-link a:hover {
      color: #5a67d8;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="icon">🔑</div>
    <h1>Recuperar Contraseña</h1>
    <p class="description">
      Ingresa tu correo electrónico y te enviaremos un enlace para restablecer tu contraseña. El enlace expirará en 5 minutos.
    </p>
    
    <div id="messageContainer"></div>
    
    <form id="forgotPasswordForm">
      <input type="email" name="email" id="email" placeholder="Correo electrónico" required>
      <button type="submit" id="submitBtn">Enviar Enlace de Recuperación</button>
    </form>
    
    <div class="back-link">
      <a href="/api/auth/login">← Volver al inicio de sesión</a>
    </div>
  </div>

  <script>
    const form = document.getElementById('forgotPasswordForm');
    const submitBtn = document.getElementById('submitBtn');
    const messageContainer = document.getElementById('messageContainer');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Limpiar mensajes previos
      messageContainer.innerHTML = '';
      
      const email = document.getElementById('email').value.trim();
      
      // Deshabilitar botón
      submitBtn.disabled = true;
      submitBtn.textContent = 'Enviando...';
      
      try {
        const res = await fetch('/api/auth/forgot-password', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email })
        });

        const data = await res.json();

        if (res.ok) {
          // Mostrar mensaje de éxito
          messageContainer.innerHTML = `
            <div class="message success">
              ✅ ${data.message}. Revisa tu bandeja de entrada y spam.
            </div>
          `;
          form.reset();
        } else {
          // Mostrar error
          messageContainer.innerHTML = `
            <div class="message error">
              ❌ ${data.message || 'Ocurrió un error. Intenta nuevamente.'}
            </div>
          `;
        }
      } catch (err) {
        console.error(err);
        messageContainer.innerHTML = `
          <div class="message error">
            ❌ Error al conectar con el servidor. Verifica tu conexión.
          </div>
        `;
      } finally {
        // Rehabilitar botón
        submitBtn.disabled = false;
        submitBtn.textContent = 'Enviar Enlace de Recuperación';
      }
    });
  </script>
</body>
</html>
