<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Dejar Reseña - Vertika</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="stylesheet" href="/static/styles/vertika-header.css">
  <style>
    .container {
      padding-top: 2rem;
      max-width: 800px;
      margin: 0 auto;
      padding-bottom: 2rem;
    }

    .card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      overflow: hidden;
      padding: 30px;
    }

    .header-section {
      text-align: center;
      margin-bottom: 30px;
    }

    .header-section h2 {
      color: #2d3748;
      margin-bottom: 10px;
    }

    .header-section p {
      color: #718096;
    }

    .rating-section {
      margin-bottom: 30px;
      text-align: center;
      background: #f7fafc;
      padding: 20px;
      border-radius: 10px;
    }

    .rating-title {
      font-weight: 600;
      color: #4a5568;
      margin-bottom: 15px;
      display: block;
    }

    .stars {
      display: inline-flex;
      flex-direction: row-reverse;
      gap: 10px;
    }

    .stars input {
      display: none;
    }

    .stars label {
      cursor: pointer;
      font-size: 2rem;
      color: #cbd5e0;
      transition: color 0.2s;
    }

    .stars input:checked ~ label,
    .stars label:hover,
    .stars label:hover ~ label {
      color: #f6e05e;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-group label {
      display: block;
      margin-bottom: 8px;
      color: #4a5568;
      font-weight: 500;
    }

    .form-control {
      width: 100%;
      padding: 12px;
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      font-size: 1rem;
      transition: border-color 0.2s;
      resize: vertical;
    }

    .form-control:focus {
      border-color: #667eea;
      outline: none;
    }

    .btn-submit {
      width: 100%;
      padding: 15px;
      background: linear-gradient(to right, #667eea, #764ba2);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.2s;
    }

    .btn-submit:hover {
      transform: translateY(-2px);
    }

    .guide-info {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 15px;
      margin-bottom: 10px;
    }

    .guide-avatar {
      width: 50px;
      height: 50px;
      background: #e2e8f0;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      color: #718096;
    }

    .image-preview-container {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 10px;
    }

    .image-preview {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 8px;
      border: 1px solid #e2e8f0;
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">
      <img src="https://imgur.com/2R9vMFY.png" onclick="window.location.href='/api'" alt="Vertika Logo" class="app-logo">
      <h1 onclick="window.location.href='/api'">VERTIKA</h1>
    </div>
  </header>

  <div class="container">
    <div class="card">
      <div class="header-section">
        <h2>¡Cuéntanos tu experiencia!</h2>
        <p>Tu opinión nos ayuda a mejorar y ayuda a otros aventureros.</p>
        <h3 style="color: #667eea; margin-top: 10px;">{{reservation.experienceId.title}}</h3>
      </div>

      <form id="reviewForm">
        <input type="hidden" name="reservationId" value="{{reservation._id}}">
        <input type="hidden" name="experienceId" value="{{reservation.experienceId._id}}">
        <input type="hidden" name="guideId" value="{{reservation.experienceId.guideId._id}}">
        <input type="hidden" name="userId" value="{{reservation.userId}}">

        <div class="rating-section">
          <span class="rating-title">Califica la Experiencia</span>
          <div class="stars">
            <input type="radio" name="experienceRating" id="exp5" value="5" required><label for="exp5"><i class="fas fa-star"></i></label>
            <input type="radio" name="experienceRating" id="exp4" value="4"><label for="exp4"><i class="fas fa-star"></i></label>
            <input type="radio" name="experienceRating" id="exp3" value="3"><label for="exp3"><i class="fas fa-star"></i></label>
            <input type="radio" name="experienceRating" id="exp2" value="2"><label for="exp2"><i class="fas fa-star"></i></label>
            <input type="radio" name="experienceRating" id="exp1" value="1"><label for="exp1"><i class="fas fa-star"></i></label>
          </div>
        </div>

        <div class="rating-section">
          <div class="guide-info">
            <div class="guide-avatar">
              {{#if reservation.experienceId.guideId.userId.avatarUrl}}
                <img src="{{reservation.experienceId.guideId.userId.avatarUrl}}" alt="Guide Avatar" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
              {{else}}
                <i class="fas fa-user"></i>
              {{/if}}
            </div>
            <div>
              <span class="rating-title" style="margin:0">Califica a tu Guía</span>
              <small style="display:block; color:#718096">{{reservation.experienceId.guideId.userId.name}}</small>
            </div>
          </div>
          <div class="stars">
            <input type="radio" name="guideRating" id="guide5" value="5" required><label for="guide5"><i class="fas fa-star"></i></label>
            <input type="radio" name="guideRating" id="guide4" value="4"><label for="guide4"><i class="fas fa-star"></i></label>
            <input type="radio" name="guideRating" id="guide3" value="3"><label for="guide3"><i class="fas fa-star"></i></label>
            <input type="radio" name="guideRating" id="guide2" value="2"><label for="guide2"><i class="fas fa-star"></i></label>
            <input type="radio" name="guideRating" id="guide1" value="1"><label for="guide1"><i class="fas fa-star"></i></label>
          </div>
        </div>

        <div class="form-group">
          <label for="comment">Comentarios</label>
          <textarea id="comment" name="comment" class="form-control" rows="5" placeholder="¿Qué fue lo que más te gustó? ¿Hay algo que podríamos mejorar?" required></textarea>
        </div>

        <div class="form-group">
          <label for="photos">Fotos (Opcional)</label>
          <input type="file" id="photos" name="photos" class="form-control" multiple accept="image/*">
          <small style="color: #718096; display: block; margin-top: 5px;">Puedes subir hasta 5 fotos.</small>
          <div id="imagePreviewContainer" class="image-preview-container"></div>
        </div>

        <button type="submit" class="btn-submit">Enviar Reseña</button>
      </form>
    </div>
  </div>
  <script>
    document.getElementById('photos').addEventListener('change', function(event) {
      const container = document.getElementById('imagePreviewContainer');
      container.innerHTML = ''; // Clear previous previews
      
      const files = event.target.files;
      if (files) {
        // Limit preview to 5 files
        const fileCount = Math.min(files.length, 5);
        
        for (let i = 0; i < fileCount; i++) {
          const file = files[i];
          if (file.type.startsWith('image/')) {
            const reader = new FileReader();
            
            reader.onload = function(e) {
              const img = document.createElement('img');
              img.src = e.target.result;
              img.classList.add('image-preview');
              container.appendChild(img);
            }
            
            reader.readAsDataURL(file);
          }
        }
      }
    });

    document.getElementById('reviewForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(e.target);
      
      // No need to convert to JSON manually, send FormData directly
      // But we need to ensure ratings are numbers if the backend expects numbers in multipart/form-data?
      // Actually, in multipart/form-data, everything is a string. 
      // Mongoose/Express usually handles type conversion if defined in schema, 
      // but let's check if we need to do anything special.
      // The controller extracts fields from req.body.
      
      try {
        const response = await fetch('/api/reviews', {
          method: 'POST',
          // Do NOT set Content-Type header, browser sets it with boundary
          body: formData
        });

        if (response.ok) {
          alert('¡Gracias por tu reseña!');
          window.location.href = '/api/reservations/my-reservations';
        } else {
          const error = await response.json();
          alert(error.error || 'Error al enviar la reseña');
        }
      } catch (err) {
        console.error(err);
        alert('Error al enviar la reseña');
      }
    });
  </script>
</body>
</html>